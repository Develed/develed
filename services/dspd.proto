syntax = "proto3";
package services;

service ImageSink {
    rpc Draw (DrawRequest) returns (DrawResponse) {}
}

message DrawRequest {
    bytes data = 1;
}

message DrawResponse {
    int32 code = 1;
    string status = 2;
}
